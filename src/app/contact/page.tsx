import ContactForm from "@/components/ContactForm";

export const metadata = {
  title: "Contact us | Coding with Andrii",
  description: "Drop us a message — we'll ignore it anyway!",
};

const Contact: React.FC = () => {
  return <ContactForm />;
};

export default Contact;
